import type { GdsThemePresetId } from "@doneisbetter/gds";

export const ATHLETE_IQ_GDS_THEME_PRESET = "athlete-gold" satisfies GdsThemePresetId;
export const ATHLETE_IQ_GOLD_LOGO_SRC = "/images/athlete-iq-gold-logo.png";

// The app selector is neutral — deliberately distinct from Habigoal (teal) and
// Athlete IQ (gold) so it reads as its own surface, not either product.
export const SELECTOR_GDS_THEME_PRESET = "skyline" satisfies GdsThemePresetId;
